import React from 'react';
import PropTypes from 'prop-types';

const Landing = () => {
    return (
        <div>
            Langing page
        </div>
    );
};

Landing.propTypes = {};

export default Landing;